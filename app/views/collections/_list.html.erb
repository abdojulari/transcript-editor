<% if collections.any?  %>
  <% collections.each do |collection| %>
    <div class="collection-item">
      <h2><%= collection.title %></h2>
      <p><strong><%= collection.library_catalogue_title %></strong></p>
      <p><%=  raw collection.description %></p>
      <p>
      <%= link_to collection.collection_url_title, collection.url %>
      </p>
    </div>
  <% end %>
<%  else  %>
  <p>No collections have been added yet!</p>
<% end %>

