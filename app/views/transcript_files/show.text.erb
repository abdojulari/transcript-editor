<% @transcript_lines.each do |line| %>
<%= "#{line.start_time_string} " if @opt[:timestamps] %><%= "<#{line.speaker_name}> " if @opt[:speakers] && !line.speaker_name.blank? %><%= "<Multiple Speakers> " if @opt[:speakers] && line.speaker_id < 0 %><%= line.best_text %>
<% end %>
