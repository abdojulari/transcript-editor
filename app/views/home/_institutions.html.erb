<% if institutions.any? %>
  <%= select_tag "institution", options_from_collection_for_select(institutions, "slug", "name", @build_params[:institution]), include_blank: (@disabled ? nil : 'All Institutions') %>
<% else %>
  No institution found
<% end %>
