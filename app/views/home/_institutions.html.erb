<%
  index = institutions.index { |e| e.id == selected_id }
  if index
    first_institution = institutions[index]
  else
    first_institution = institutions.first
  end
%>
<% if institutions.any? %>
  <%= select_tag "data[institution_id]", options_from_collection_for_select(institutions, "id", "name", index) %>
<% else %>
  No institution found
<% end %>


